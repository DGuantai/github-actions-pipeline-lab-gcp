name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show release tag
        run: |
          echo "Release triggered for tag: $GITHUB_REF_NAME"