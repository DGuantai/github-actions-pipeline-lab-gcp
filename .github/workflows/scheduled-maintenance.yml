name: Scheduled Maintenance

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

jobs:
  maintenance-check:
    runs-on: ubuntu-latest

    steps:
      - name: Maintenance task
        run: |
          echo "Running scheduled maintenance checks"
          date